# Server
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL=postgres://user:pass@localhost:5432/autoloan_development # pragma: allowlist secret

# JWT
JWT_SECRET=your-secret-key-change-in-production # pragma: allowlist secret

# SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password # pragma: allowlist secret

# Frontend
FRONTEND_URL=http://localhost:5173

# Security
BLOCKED_IPS=
